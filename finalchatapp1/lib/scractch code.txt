Navigator.push(
                    context,
                    MaterialPageRoute(
                        builder: (context) =>
                            AddDataToFireStore()), // mywidget is in newclass
                  );